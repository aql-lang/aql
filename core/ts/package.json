{
  "name": "@boru-lang/core",
  "version": "0.1.0",
  "description": "TypeScript port of the boru interpreter core \u2014 values, types, signatures, matching, the registry, and the step loop. The TS twin of the core/go module: no check pass, no compiler, no parser, no dependencies.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "typecheck": "tsc",
    "test": "node --test --experimental-strip-types --no-warnings 'src/**/*.test.ts'"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "@types/node": "^24.7.0"
  },
  "files": [
    "dist",
    "src"
  ]
}
