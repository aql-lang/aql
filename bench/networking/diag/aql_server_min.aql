import "aql:net"
import "aql:time-util"
def ln (Net.serve-raw {tcp: 19095} (fn [[sock:Any] [Any] [
  def nl (convert Bytes "\n")
  def reply (convert Bytes "hello world\n")
  for 40000 [
    Net.recv-until sock nl drop
    Net.send-bytes reply sock
  ]
]]))
TimeUtil.sleep 60000
