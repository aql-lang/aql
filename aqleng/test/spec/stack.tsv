# Stack manipulation via test-registered `dup`, `swap`, `drop`, `neg`.
# `neg` is registered without forward precedence (stack-only) so it
# must consume the prefix value.

# tokens	expected	note
1 dup	1 1	dup duplicates the top
1 2 dup	1 2 2	dup affects only the top
1 2 swap	2 1	swap exchanges the top two
1 2 3 swap	1 3 2	swap touches only the top two
1 2 drop	1	drop removes the top
1 dup drop	1	dup then drop is a no-op
5 neg	-5	stack-only word consumes prefix
3 neg neg	3	double negation
1 2 neg	1 -2	neg only touches its arg
