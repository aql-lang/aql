# Stack manipulation via test-registered `dup`, `swap`, `drop`.
# These three are stack-only by default — they consume only from
# the prefix and never collect forward args.
#
# (Negation moved to decimals.tsv after the neg/negate consolidation
# into a single forward-eligible `negate` word with sig [Number|]).

# tokens	expected	note
1 dup	1 1	dup duplicates the top
1 2 dup	1 2 2	dup affects only the top
1 2 swap	2 1	swap exchanges the top two
1 2 3 swap	1 3 2	swap touches only the top two
1 2 drop	1	drop removes the top
1 dup drop	1	dup then drop is a no-op
