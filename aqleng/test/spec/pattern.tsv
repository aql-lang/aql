# §1.1 literal-pattern dispatch via Signature.patterns.
#
# The engine routes specific-value dispatch through a per-signature
# `patterns` map keyed by arg position. A concrete-scalar pattern
# (Data != null on a numeric / string / boolean / atom) matches when
# the arg's Data equals the pattern's Data; if it doesn't, the sig is
# skipped and a less-specific overload (typically with no patterns) is
# tried next.
#
# Test words:
#   fact — two overloads on [Integer]:
#            patterns:{0: 0}  → returns 1   (base case)
#            no patterns      → returns the arg unchanged (catch-all)
#   codeq — two overloads on [Integer]:
#            patterns:{0: 99} → returns "ninety-nine"
#            no patterns      → returns "general"
#   routeq — two overloads on [String]:
#            patterns:{0: "admin"} → returns "matched-admin"
#            no patterns           → returns "other"

# tokens	expected	note
factq 0	1	pattern 0 matches → base-case overload
factq 5	5	non-zero falls through to catch-all → echoes the arg
factq 1	1	1 fails the {0:0} pattern, catch-all returns 1 unchanged
codeq 99	"ninety-nine"	pattern 99 matches → specific string
codeq 42	"general"	non-99 falls through to catch-all
routeq "admin"	"matched-admin"	string-literal pattern matches on Data equality
routeq "user"	"other"	non-admin string falls through
