# parser/spec/divergent.tsv — the parser parity-debt ledger.
#
# One row per divergence: src <TAB> go <TAB> ts <TAB> justification.
# Same escaping and comment rules as parse.tsv. The file is intentionally
# empty of data rows: parser/go and parser/ts currently agree on every pinned
# parser contract. New rows require the reviewed shrink-only justification
# described in parser/spec/README.md.
[1 =>	[paren([1 sugar(lambda) 1])]	[1]	trailing `=>` with no body: the Go arrowfold still folds (the input doubles as the body); the TS fold drops the paren group from the value stream entirely. A DATA-class divergence — Go matches the fold-always arrowfold grammar.
if . `${x}` =>	if.(interp(${word(x)}) sugar(lambda) interp(${word(x)}))	if.interp(${word(x)})	the same trailing-arrow fold loss, inside a dotchain segment: Go folds the interp into a lambda group, TS keeps only the bare interp.
? ? :	word(?) word(?)	ERR [boru/syntax_error]: empty list element: remove the leading/repeated comma (write `none` for an explicit empty value)	a trailing bare colon: Go accepts and drops the empty implicit pair, TS refuses the whole source — an accept/reject split.
def x 2 => ,	ERR [boru/syntax_error]: empty list element: remove the leading/repeated comma (write `none` for an explicit empty value)	word(def) word(x) 2	`=>` then `,`: Go refuses the empty element, TS accepts and silently drops both trailing tokens — an accept/reject split in the opposite direction from the bare-colon row.
y 0 ] Map	ERR [boru/syntax_error]: unexpected `Map` — nothing valid can appear here	ERR [boru/syntax_error]: unexpected `` — nothing valid can appear here	after an unmatched `]`, Go names the offending token in the recovery diagnostic; TS reports an empty token. Same code, divergent detail and location.
.. (	ERR [boru/syntax_error]: `.` member access has no receiver	ERR [boru/syntax_error]: unmatched opening parenthesis	error-precedence: Go reports the receiverless `.`, TS the unmatched `(` — the two walks meet the faults in a different order.
/s 1e400 ( #c	ERR empty word	ERR [boru/syntax_error]: unmatched opening parenthesis	Go refuses the bare `/s` modifier first (`empty word`); TS reports the unmatched `(`. Error-precedence divergence with a non-BoruError Go render.
quote . ( =	ERR unsupported value type parser.unclosedParen	ERR unsupported value type UnclosedParen	both ports leak an internal marker type name into the message (`parser.unclosedParen` vs `UnclosedParen`) — neither render is a stable user-facing diagnostic.
