# parser/spec/divergent.tsv — the parser parity-debt ledger.
#
# One row per divergence: src <TAB> go <TAB> ts <TAB> justification.
# Same escaping and comment rules as parse.tsv. The file is intentionally
# empty of data rows: parser/go and parser/ts currently agree on every pinned
# parser contract. New rows require the reviewed shrink-only justification
# described in parser/spec/README.md.
